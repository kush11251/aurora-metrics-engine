# Aurora Metrics Engine
A high-performance metrics aggregation and analysis platform.

## Running the Application
1. Install dependencies with `npm install`.
2. Start the application with `node src/index.js`.
